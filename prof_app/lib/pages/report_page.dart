import 'package:flutter/material.dart';

class ReportPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
        appBar: AppBar(
          title: Text('Страница отчета'),
          centerTitle: true,
          backgroundColor: Colors.orange[600],
        ),
      );
}
