import 'package:flutter/material.dart';

class HistoryPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
        appBar: AppBar(
          title: Text('История ПРОФа'),
          centerTitle: true,
          backgroundColor: Colors.orange[600],
        ),
      );
}
