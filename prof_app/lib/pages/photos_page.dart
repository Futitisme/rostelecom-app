import 'package:flutter/material.dart';

class PhotosPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
        appBar: AppBar(
          title: Text('Наши фото'),
          centerTitle: true,
          backgroundColor: Colors.orange[600],
        ),
      );
}
