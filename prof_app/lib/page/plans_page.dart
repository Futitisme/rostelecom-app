import 'package:flutter/material.dart';

class PlansPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
        appBar: AppBar(
          title: Text('Наши планы'),
          centerTitle: true,
          backgroundColor: Colors.orange[600],
        ),
      );
}
