import 'package:flutter/material.dart';

class ApplyPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
        appBar: AppBar(
          title: Text('Страница подачи заявки'),
          centerTitle: true,
          backgroundColor: Colors.orange[600],
        ),
      );
}
